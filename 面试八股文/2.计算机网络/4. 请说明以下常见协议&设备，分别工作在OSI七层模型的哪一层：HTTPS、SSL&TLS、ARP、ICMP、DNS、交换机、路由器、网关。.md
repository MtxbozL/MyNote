参考答案：
 
- HTTPS：应用层（七层），HTTP+SSL/TLS的组合，核心业务逻辑在应用层
​
- SSL/TLS：表示层（六层），负责数据加密解密、格式安全处理
​
- ARP：数据链路层（二层），通过IP地址解析MAC地址
​
- ICMP：网络层（三层），用于网络连通性检测、差错报告（如ping、traceroute）
​
- DNS：应用层（七层），实现域名与IP地址的解析转换
​
- 交换机：数据链路层（二层），三层交换机可支持网络层（三层）
​
- 路由器：网络层（三层）
​
- 网关：通常工作在应用层（七层），如API网关、代理网关，也可根据场景支持四层转发