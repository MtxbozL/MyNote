## 一、基础技术题（必答，考察基本功）

### -> 请说出你最常用的 5 个 Linux 进程管理命令和 5 个网络排查命令，并分别说明它们的核心用途和 1 个典型使用场景。

```bash
### 一、5 个核心进程管理命令

1. top：实时监控系统 CPU、内存、负载和进程资源占用;
   典型场景：服务器卡顿，第一时间按 P/M 排序定位异常进程
2. ps -ef/ps aux：静态查看所有进程的 PID、启动用户和命令;
   典型场景：`ps -ef | grep nginx`确认服务是否启动
3. kill/kill -9：向进程发送信号终止运行;
   典型场景：进程卡死时，先用 ps 查 PID，再用`kill -9 PID`强制终止
4. pstree -p：树形展示进程父子关系;
   典型场景：排查僵尸进程，快速定位其父进程
5. htop：top 增强版，彩色界面支持鼠标操作;
   典型场景：长时间监控进程或批量终止同类进程

### 二、5 个核心网络排查命令

1. ss -tulnp：新一代端口查看工具，速度更快，
   **典型场景**：`ss -tulnp | grep 3306`检查 MySQL 端口是否正常监听
2. netstat：老系统兼容的网络信息工具，
   **典型场景**：`netstat -an | grep ESTABLISHED | wc -l`统计 TCP 连接数
3. ping：测试 ** 三层（网络层）** 连通性，
   **典型场景**：服务无法访问时，先 ping 网关和目标 IP，定位网络故障范围
4. mtr：结合 ping 和 traceroute 的路由追踪工具，
   **典型场景**：跨机房访问延迟高，定位丢包的中间路由节点
5. telnet/nc：测试 ** 四层（传输层）** 端口连通性，
   **典型场景**：服务启动后客户端连不上，快速区分是网络问题还是服务本身问题
```

### -> 编写一个 Shell 脚本，实现每天凌晨 2 点自动清理`/var/log`目录下 7 天前的`.log`文件，同时记录清理的文件数量和总大小到`/var/log/clean_log.log`。

> 面试官您好，这个需求我分两步实现：先写健壮的独立脚本，再用 crontab 配置定时任务。

#### 核心实现思路

1. 用变量统一定义清理目录、保留天数、日志路径，便于后续维护
2. 先检查目标目录是否存在，避免脚本异常
3. 先统计待清理的文件数量和总大小，再执行删除
4. 用`-delete`替代`rm -rf`，更安全高效
5. 所有操作记录到日志文件，便于排错

#### 核心脚本代码（只记关键行）

```bash
#!/bin/bash
LOG_DIR="/var/log"
RETENTION_DAYS=7
LOG_FILE="/var/log/clean_log.log"

echo "===== $(date '+%Y-%m-%d %H:%M:%S') 开始 =====" >> $LOG_FILE
[ ! -d "$LOG_DIR" ] && echo "目录不存在" >> $LOG_FILE && exit 1

FILE_COUNT=$(find "$LOG_DIR" -type f -name "*.log" -mtime +7 | wc -l)
FILE_SIZE=$(find "$LOG_DIR" -type f -name "*.log" -mtime +7 | xargs du -ch | tail -1 | awk '{print $1}')

[ $FILE_COUNT -gt 0 ] && find "$LOG_DIR" -type f -name "*.log" -mtime +7 -delete && echo "清理$FILE_COUNT个文件，大小$FILE_SIZE" >> $LOG_FILE
echo "===== $(date '+%Y-%m-%d %H:%M:%S') 结束 =====" >> $LOG_FILE
```

#### 定时任务配置

1. 加执行权限：`chmod +x /usr/local/bin/clean_log.sh`
2. 编辑 root 的 crontab：`sudo crontab -e`
3. 添加任务：`0 2 * * * /usr/local/bin/clean_log.sh`

### -> GitFlow 工作流中，`feature`、`develop`、`master`、`hotfix`分支分别用于什么场景？请描述一次你解决 Git 合并冲突的具体过程。

> 面试官您好，GitFlow 是标准化的分支管理工作流，四个核心分支的用途和我解决合并冲突的过程如下：

#### 一、4 个核心分支的使用场景

1. **`master`**：**生产环境分支**，只能合并不能直接提交，存放随时可上线的稳定代码，每次合并都要打版本标签
2. **`develop`**：**开发环境主分支**，集成所有开发完成的功能，是下一个版本的代码基础
3. **`feature`**：**功能开发分支**，从`develop`拉取，每个新功能单独建一个，开发完成后合并回`develop`
4. **`hotfix`**：**紧急修复分支**，从`master`拉取，修复线上紧急 bug，修复完成后同时合并回`master`和`develop`

#### 二、Git 合并冲突的具体解决过程

1. 先拉取远程最新代码：`git pull origin develop`，此时 Git 会提示冲突文件
2. 打开冲突文件，找到`<<<<<<< HEAD`（本地代码）、`=======`（分隔线）、`>>>>>>> 远程分支名`（远程代码）标记的冲突区域
3. 对比两边代码，保留正确的部分，删除所有冲突标记
4. 保存文件后，执行`git add 冲突文件`标记冲突已解决
5. 最后执行`git commit -m "解决合并冲突"`完成提交

#### 补充得分点

> 我一般会在合并前先拉取远程最新代码，尽量减少冲突；如果冲突比较复杂，会用 VSCode 的 Git 插件可视化对比，效率更高。

### -> Ansible 的核心组件有哪些？如何使用 Ansible 批量在 100 台 CentOS 服务器上安装并启动 Nginx 服务？

> 面试官您好，Ansible 的核心组件和批量安装 Nginx 的步骤如下：

#### 一、Ansible 核心组件

1. **Inventory（主机清单）**：定义需要管理的目标主机和分组
2. **Modules（模块）**：Ansible 的执行单元，比如`yum`、`systemd`、`copy`等
3. **Ad-Hoc**：临时命令执行工具，用于快速执行单次简单任务
4. **Playbook**：剧本，用 YAML 编写，定义批量执行的任务流程
5. **Ansible Core**：Ansible 的核心程序，负责解析和执行任务

#### 二、批量在 100 台 CentOS 上安装启动 Nginx 的步骤

1. **配置免密登录**（Ansible 基于 SSH）
    
    ```bash
    # 生成密钥对
    ssh-keygen -t rsa
    # 批量推送公钥到所有目标主机
    ssh-copy-id root@目标主机IP
    ```
    
2. **配置 Inventory 主机清单**（`/etc/ansible/hosts`）
    
    ```bash
    [web]
    192.168.1.101
    192.168.1.102
    # ... 共100台服务器IP
    ```
    
3. **验证主机连通性**
    
    ```bash
    ansible web -m ping
    ```
    
4. **编写 Playbook 剧本**（`install_nginx.yml`）
    
    ```yaml
    - name: 批量安装并启动Nginx
      hosts: web
      become: yes
      tasks:
        - name: 安装Nginx
          yum: name=nginx state=present
        - name: 启动Nginx并设置开机自启
          systemd: name=nginx state=started enabled=yes
        - name: 开放80端口防火墙
          firewalld: port=80/tcp permanent=yes state=enabled immediate=yes
    ```
    
5. **执行 Playbook**
    
    ```bash
    ansible-playbook install_nginx.yml
    ```
    

#### 补充得分点

> 我会在 Playbook 里增加配置文件同步和版本校验的步骤，确保所有服务器的 Nginx 配置一致；如果服务器数量更多，会用 Ansible 的并行执行参数`-f`提高效率。

## 二、核心技能进阶题（分模块，考察实操与问题解决能力）

### 模块 1：MySQL 与 Redis

### -> MySQL 主从同步的完整原理是什么？主从同步延迟超过 10 分钟，你会按照什么顺序排查原因？至少说出 3 种常见解决方案。

>理解主从同步的 **三个核心线程**

#### 一、主从同步完整原理（核心：三个线程）

1. **Master 节点**：有一个`dump 线程`，当 slave 连接后，负责读取 binlog 日志，推送给 slave 的 IO 线程
2. **Slave 节点**：有两个线程
    
    - `IO线程`：接收 master 推送的 binlog，写入本地的`relay log（中继日志）`
    - `SQL线程`：读取中继日志，重放里面的 SQL 命令，实现数据同步

#### 二、主从延迟 10 分钟的排查顺序（从易到难）

![[Pasted image 20260417163902.png]]

1. 先看 **从节点 SQL 线程状态** ：`show slave status\G`，看`Seconds_Behind_Master`和`SQL_Running`是否正常
2. 再看**主节点负载**：用 top 看 CPU、IO、内存，是否有大事务、慢查询导致 binlog 生成过慢
3. 再看**从节点负载**：是否有大量读请求占用资源，导致 SQL 线程执行慢
4. 最后看**网络情况**：用 mtr 测试主从之间的网络延迟和丢包

#### 三、3 种常见解决方案

1. 优化主节点：避免大事务，拆分长 SQL 为小 SQL；开启 binlog 行模式
2. 优化从节点：关闭从节点的慢查询日志；增加从节点硬件配置
3. 架构优化：采用一主多从分担读压力；使用 **半同步复制** 减少数据丢失风险

### -> Redis 的 RDB 和 AOF 持久化有什么本质区别？如果线上 Redis 服务器突然宕机，如何保证数据尽可能不丢失？

>**数据完整性、恢复速度、性能影响**三个核心维度
#### 一、RDB 与 AOF 的核心区别

| 维度    | RDB                 | AOF                  |
| ----- | ------------------- | -------------------- |
| 存储内容  | 某一时刻的 **全量数据快照**    | 所有 **写操作命令** 的日志     |
| 数据完整性 | 差，会丢失最后一次快照后的数据     | 好，可配置每秒刷盘，最多丢失 1 秒数据 |
| 恢复速度  | 快，适合全量恢复            | 慢，需要重放所有命令           |
| 性能影响  | 小，fork 子进程执行，不阻塞主进程 | 大，每次写操作都要刷盘          |

#### 二、线上 Redis 宕机保证数据不丢失的方案

![[Pasted image 20260417162902.png]]

1. **开启混合持久化**（Redis 4.0 + 默认推荐）：结合 RDB 和 AOF 的优点，用 RDB 做全量备份，AOF 记录增量命令，恢复速度快且数据丢失少
2. **配置持久化参数**：AOF 设置`appendfsync everysec`（每秒刷盘，性能和安全平衡）；RDB 设置合理的自动快照频率
3. **部署主从集群**：主节点宕机后，从节点可以快速切换，保证服务不中断
4. **定期手动备份**：每天凌晨备份 RDB 文件到异地存储，防止极端情况


### -> 什么是缓存击穿、缓存雪崩、缓存穿透？请分别说明它们的触发场景和你在实际工作中使用过的解决方案。

> 面试官您好，这三个问题的触发场景和我实际用过的解决方案如下：

7. **缓存穿透**
    
    - 触发场景：用户查询 **数据库和缓存中都不存在** 的 key，导致所有请求直接打数据库
    - 解决方案：我用过 **布隆过滤器** ，把所有存在的 key 提前存入布隆过滤器，请求先过过滤器，不存在的直接拦截；同时对数据库返回的空结果也缓存 5 分钟
    
8. **缓存击穿**
    
    - 触发场景：**某个热点 key 突然过期**，大量并发请求同时打数据库
    - 解决方案：我用过 **互斥锁**，当缓存失效时，只有第一个请求能获取锁去查数据库并更新缓存，其他请求等待缓存更新完成；另外对热点 key 设置 **永不过期** 
    
9. **缓存雪崩**
    
    - 触发场景：① 大量 key **同一时间集体过期** ；
	           ② Redis 服务**整体宕机**
    - 解决方案：① 给 key 的过期时间加 **随机值**，避免同时过期；
               ② 部署 Redis 主从集群 + 哨兵，实现自动故障转移；
               ③ 服务端加 **熔断和降级**，Redis 宕机时暂时返回默认值

### -> 如何定位 MySQL 的慢查询？请说出至少 3 种慢查询优化方法，并说明各自的适用场景。

![[Pasted image 20260417174552.png]]

> 面试官您好，我定位和优化 MySQL 慢查询的方法如下：

#### 一、定位慢查询的 3 种方法

1. **开启慢查询日志**：设置`long_query_time=1`，让 MySQL 自动记录执行超过 1 秒的 SQL，这是生产环境最常用的方法
2. **工具分析**：用`pt-query-digest`批量分析慢查询日志，按总耗时和执行次数排序，快速找出 Top 慢 SQL
3. **实时排查**：紧急故障时用`show full processlist`查看当前正在执行的 SQL，找到耗时最长的进程

#### 二、3 种核心优化方法及适用场景

1. **加索引**：给`WHERE`、`JOIN`、`ORDER BY`后面的字段加合适的索引，这是成本最低、效果最好的优化，适用于绝大多数没有索引的慢查询
2. **优化 SQL**：避免`SELECT *`、避免字段上用函数、拆分长 SQL，适用于 SQL 写法不规范导致的慢查询
3. **架构优化**：当 SQL 和索引优化到极致后，采用读写分离、Redis 缓存、分库分表等架构方案，适用于高并发、大数据量的场景

#### 补充得分点（一句话带过，体现实操经验）

> 我一般会在生产环境开启慢查询日志，每天凌晨用定时任务自动分析并生成报告，定期和开发一起优化 Top 慢 SQL；同时会避免给更新频繁的字段加索引，因为索引会降低写性能。


## 模块 2：Nginx 与 Web 服务

### Nginx 如何实现反向代理和负载均衡？常用的负载均衡策略有哪些？什么场景下适合使用 ip_hash 策略？
>1. 不能只说明使用了哪个模块，需要说清楚反向代理和负载均衡是如何配合工作的。
>2. 需要说明哪个是默认策略，同时讲清楚每个策略的核心特点。

> 面试官您好，Nginx 反向代理和负载均衡的实现、常用策略及 ip_hash 的适用场景如下：

#### 一、反向代理和负载均衡的实现

Nginx 通过两个核心模块配合实现：

1. 用`upstream`模块定义后端服务器池，把所有提供相同服务的后端节点配置在一起
2. 用`server`模块的`location`匹配请求，通过`proxy_pass`指令把请求转发到 upstream 定义的服务器池，从而实现反向代理和负载均衡

#### 二、常用的 5 种负载均衡策略

![[Pasted image 20260417170226.png]]

1. **轮询（默认）**：请求按顺序依次分配到不同的后端服务器
2. **加权轮询**：权重越高的服务器，分配到的请求越多，适用于后端服务器性能不均的场景
3. **ip_hash**：根据客户端 IP 的哈希结果分配服务器，同一个客户端的请求始终分配到同一个后端服务器 **(会话保持)**
4. **url_hash**：根据请求 URL 的哈希结果分配服务器，适用于静态资源缓存场景
5. **least_conn**：请求分配到当前连接数最少的服务器，适用于长连接较多的场景

#### 三、ip_hash 的适用场景

**核心场景：需要会话保持的业务**，比如用户登录状态、购物车等。因为同一个用户的请求始终打到同一台后端服务器，就不需要在多台服务器之间同步会话数据了。

### 配置 Nginx HTTPS 需要哪些步骤？如何强制将 HTTP 请求跳转到 HTTPS？

> 1. **步骤完善**： 除了 listen ssl 还需要提及 提证书申请、证书配置、SSL 协议优化等核心步骤。
> 2. **没有实操细节**：强制跳转不能只说了指令，要给生产环境标准的配置示例。
> 3. **遗漏关键注意事项**：比如 443 端口开放、证书权限等常见坑

> 面试官您好，配置 Nginx HTTPS 和强制跳转的步骤如下：

#### 一、HTTPS 完整配置步骤

1. **申请 SSL 证书**：可以从阿里云、腾讯云等平台申请免费的 Let's Encrypt 证书，得到`证书文件(.crt)`和`私钥文件(.key)`
2. **上传证书到服务器**：一般放在`/etc/nginx/ssl/`目录下，设置证书权限为 600（避免私钥泄露）
3. **修改 Nginx 配置文件**，在 server 块中添加以下配置：
    
    ```nginx
    server {
        listen 443 ssl;
        server_name yourdomain.com;
        
        # 配置证书路径
        ssl_certificate /etc/nginx/ssl/yourdomain.crt;
        ssl_certificate_key /etc/nginx/ssl/yourdomain.key;
        
        # 优化SSL协议和加密套件（提升安全性和性能）
        ssl_protocols TLSv1.2 TLSv1.3;
        ssl_ciphers ECDHE-RSA-AES128-GCM-SHA256:HIGH:!aNULL:!MD5:!RC4:!DHE;
        
        # 其他常规配置
        location / {
            proxy_pass http://backend;
        }
    }
    ```
    
4. 检查配置并重启 Nginx：`nginx -t && systemctl restart nginx`
5. 开放服务器 443 端口防火墙

#### 二、HTTP 强制跳转 HTTPS（生产环境标准写法）

单独写一个 80 端口的 server 块，用`return 301`永久重定向（比 rewrite 性能更好）：

```nginx
server {
    listen 80;
    server_name yourdomain.com;
    return 301 https://$server_name$request_uri;
}
```

### Nginx 出现`502 Bad Gateway`错误，可能有哪些原因？请写出你的完整排查流程。

> 排查思路应该为 **从易到难、从常见到罕见**。

![[Pasted image 20260417171259.png]]

> 面试官您好，Nginx 出现 502 Bad Gateway，本质是 Nginx 转发请求后，没有收到后端服务的正常响应。我会按照以下顺序排查：

1. **第一步：先确认后端服务是否正常运行**（90% 的 502 都是这个原因）
    
    - 登录后端服务器，用`ps -ef | grep 服务名`查看进程是否存在
    - 本地测试后端服务：`curl http://127.0.0.1:端口号`，看是否能正常返回结果
    
2. **第二步：检查 Nginx 与后端服务器的网络连通性**
    
    - 在 Nginx 服务器上，用`telnet 后端IP 后端端口`或`nc -zv 后端IP 后端端口`测试端口是否能通
    - 检查两端服务器的防火墙和安全组，是否开放了后端服务的端口
    
3. **第三步：检查 Nginx 配置是否正确**
    
    - 用`nginx -t`检查配置文件语法是否正确
    - 确认`proxy_pass`指向的后端地址和端口是否正确
    - 检查是否配置了错误的负载均衡策略或健康检查
    
4. **第四步：检查后端服务资源是否不足**
    
    - 用`top`、`free`、`df`查看后端服务器的 CPU、内存、磁盘使用率
    - 如果资源耗尽，后端服务会无法响应新的请求，导致 502
    
5. **第五步：检查 Nginx 的超时设置**
    
    - 如果后端服务处理请求过慢，超过了 Nginx 的超时时间，也会返回 502
    - 可以适当调大`proxy_connect_timeout`、`proxy_read_timeout`等超时参数

### 你是如何实现 Nginx 动静分离的？相比不做动静分离，能带来哪些具体的性能提升？

> 面试官您好，我是通过 Nginx 的 location 匹配规则实现动静分离的，具体如下：

#### 一、动静分离的具体实现

静态资源（图片、CSS、JS、HTML 等）直接由 Nginx 处理，动态请求（.php、.jsp、接口等）转发给后端应用服务器。配置示例：

```nginx
server {
    listen 80;
    server_name yourdomain.com;
    
    # 静态资源由Nginx直接处理
    location ~* \.(jpg|jpeg|png|gif|css|js|html)$ {
        root /usr/share/nginx/static;  # 静态资源存放目录
        expires 7d;  # 设置7天缓存，提升二次访问速度
    }
    
    # 动态请求转发给后端Tomcat
    location / {
        proxy_pass http://tomcat_backend;
    }
}
```

#### 二、动静分离带来的具体性能提升

![[Pasted image 20260417171711.png|632]]

1. **大幅降低后端应用服务器的压力**：静态资源占网站请求的 80% 以上，全部由 Nginx 处理，后端只需要处理 20% 的动态请求
2. **提高静态资源的响应速度**：Nginx 处理静态资源的性能远高于 Tomcat、Node.js 等应用服务器
3. **利用 Nginx 的缓存能力**：静态资源可以设置长时间缓存，用户二次访问时直接从浏览器缓存或 Nginx 缓存返回，减少网络请求
4. **提升网站整体并发能力**：Nginx 可以轻松支持几万甚至几十万的并发连接，比应用服务器的并发能力强得多

## 模块 3：Docker 与 Kubernetes

### Dockerfile 中`RUN`、`CMD`、`ENTRYPOINT`三个指令的核心区别是什么？如果需要容器启动时执行多个命令，应该如何配置？

> 面试官您好，这三个指令的 **执行时机** 和 **作用** 完全不同：

1. **RUN**：**构建镜像时** 执行，用于安装软件、配置环境，每一个 RUN 都会生成一个新的镜像层

2. **CMD**：**容器启动时** 执行，是默认命令和参数，可以被`docker run`后面的命令完全覆盖

3. **ENTRYPOINT**：**容器启动时** 执行，是主程序入口，`docker run`后面的参数会作为它的参数，不会被覆盖

- 我一般会组合使用：ENTRYPOINT 指定主程序，CMD 指定默认参数，这样既灵活又规范

### Kubernetes 中`Pod`、`Deployment`、`Service`、`Ingress`的作用分别是什么？它们之间是如何协同工作的？

>面试官您好，这四个核心组件的作用和协同工作流程如下：

1. **Pod**：K8s 最小部署单元，封装容器，提供网络和存储
2. **Deployment**：管理 Pod 的生命周期，定义 Pod 的副本数和更新策略，保证 Pod 的高可用
3. **Service**：为一组相同的 Pod 提供固定的集群内访问地址，实现负载均衡
4. **Ingress**：集群对外的 HTTP/HTTPS 入口，根据域名和路径将请求转发到不同的 Service

协同流程：用户请求先到 Ingress，Ingress 转发到对应的 Service，Service 再把请求负载均衡到后端的多个 Pod 上，Deployment 负责保证这些 Pod 始终有指定数量在运行。

### Docker 多阶段构建的核心优势是什么？请结合你简历中提到的前端项目，写出一个简单的多阶段构建 Dockerfile 示例。

> 面试官您好，多阶段构建的核心优势是 **大幅减小最终镜像的体积**，同时避免把构建工具和源码带到生产环境，更安全。

我在之前的 CI/CD 项目中，给前端项目写的多阶段构建 Dockerfile 示例如下：

```dockerfile
# 第一阶段：构建阶段，用node镜像打包前端代码
FROM node:16-alpine AS builder
WORKDIR /app
COPY package*.json ./
RUN npm install
COPY . .
RUN npm run build  # 生成dist目录，里面是编译后的静态资源

# 第二阶段：运行阶段，只保留nginx和编译后的静态资源
FROM nginx:alpine
COPY --from=builder /app/dist /usr/share/nginx/html
EXPOSE 80
CMD ["nginx", "-g", "daemon off;"]
```

> 这样最终的镜像只有几十 MB，而单阶段构建会有几百 MB，不仅传输快，部署也更快。

### Kubernetes 集群中一个 Pod 一直处于`Pending`状态，你会从哪些方面排查问题？

![[Pasted image 20260417172928.png]]

>面试官您好，Pod 处于 Pending 状态，我会按照以下顺序排查：

4. **第一步：查看 Pod 的详细事件**（最关键，90% 的问题都能在这里找到）
    
    ```bash
    kubectl describe pod <pod-name> -n <namespace>
    ```
    
    **看 Events 字段**，常见错误有：资源不足、镜像拉取失败、端口冲突
    
5. **第二步：检查集群资源是否充足**
    
    用`kubectl top nodes`查看节点的 CPU 和内存使用率，如果所有节点资源都满了，Pod 就无法被调度
    
6. **第三步：检查镜像是否能正常拉取**
    
    确认镜像地址是否正确、私有镜像仓库的密钥是否配置、节点是否能访问镜像仓库
    
7. **第四步：检查调度策略是否正确**
    
    比如是否配置了 **节点亲和性、污点和容忍度**，导致 Pod 无法调度到任何节点
    
8. **第五步：检查端口是否冲突**
    
    如果 Pod 使用了 hostPort，确认该端口在目标节点上没有被占用

## 模块 4：CI/CD 与自动化运维

### Jenkins 的声明式 Pipeline 和脚本式 Pipeline 有什么区别？你在项目中使用的是哪一种？为什么？

> 通俗理解
- **脚本式 Pipeline**：像写自由格式的 Groovy 脚本，怎么写都行，非常灵活，但写出来的东西别人很难看懂，出了问题也不好排错
- **声明式 Pipeline**：像填标准化的表格，有固定的结构和关键字，所有人写出来的格式都一样，易读易维护，Jenkins 还能帮你做语法检查

> 面试官您好，这两种 Pipeline 的核心区别和我的选择如下：

#### 一、核心区别（3 个最关键的）

|维度|脚本式 Pipeline|声明式 Pipeline|
|---|---|---|
|语法结构|基于 Groovy 的自由脚本，无固定结构|有严格的固定结构，必须包含`pipeline`、`agent`、`stages`、`steps`等关键字|
|易用性|门槛高，需要掌握 Groovy 语法|门槛低，结构化清晰，新手也能快速上手|
|可维护性|差，复杂逻辑写出来像天书，团队协作困难|好，统一的格式，所有人都能看懂，出问题容易定位|

#### 二、我在项目中使用的是**声明式 Pipeline**

原因有 3 点：

1. 结构化清晰，每个阶段（拉取代码、构建、打包、部署、通知）都分开写，团队成员都能看懂和修改
2. 有内置的语法检查，写的时候就能发现错误，避免运行时才出问题
3. 支持很多声明式指令，比如`post`阶段可以统一处理构建成功 / 失败的通知，不用自己写复杂的逻辑

#### 补充得分点

> 脚本式 Pipeline 我也了解，它更灵活，适合处理非常复杂的自定义逻辑，但对于我们常规的 CI/CD 流水线来说，声明式 Pipeline 完全够用，而且维护成本低很多。

### 基于 Jenkins 搭建 CI/CD 流水线时，如何实现代码提交到 GitLab 后自动触发构建？

>通俗理解
原理就是：GitLab 有一个 "通知功能"（WebHook），当有人提交代码时，GitLab 会给 Jenkins 发一个 HTTP 请求，告诉 Jenkins"有人提交代码了，你赶紧构建吧"。

> 面试官您好，我在之前的 Jenkins CI/CD 项目中就是通过 **GitLab WebHook** 实现自动触发构建的，具体步骤如下：

#### 一、第一步：Jenkins 端配置

1. 先在 Jenkins 上安装`GitLab Plugin`插件，重启 Jenkins 生效
2. 进入你的流水线任务，点击 "配置"
3. 在 "构建触发器" 部分，勾选 **"触发远程构建"**，生成一个随机的**身份验证令牌 **（比如`jenkins123456`）
4. 保存配置，此时你会得到一个触发 URL：`http://你的Jenkins地址/job/任务名/build?token=jenkins123456`

#### 二、第二步：GitLab 端配置

1. 进入你的 GitLab 项目，点击 "设置"→"WebHook"
2. 在 "URL" 处填写上面得到的 Jenkins 触发 URL
3. 在 "触发器" 部分，勾选 **"Push events"**（代码提交时触发）
4. 可以勾选 "分支过滤"，只让特定分支（比如 develop、master）的提交触发构建
5. 点击 "添加 WebHook" 保存

#### 三、第三步：测试验证

随便提交一行代码到 GitLab，回到 Jenkins，就能看到流水线已经自动开始构建了。

#### 补充得分点（体现实操经验）

> 我当时还在流水线的`post`阶段配置了 WebHook 通知，把构建和部署的结果实时推送到企业微信，运维人员不用盯着 Jenkins 就能知道部署状态，这也是我简历里提到的 "故障响应效率提升 80%" 的一个重要原因。
> 
> 另外要注意两个常见坑：一是要确保 GitLab 服务器能访问到 Jenkins 服务器；二是不要把令牌泄露出去，避免被恶意触发构建。

1. 你在简历中提到使用 FastAPI 搭建运维 API 服务，请说明你是如何设计接口权限控制的？如何保证 API 服务的高可用？

```bash

```

1. 如何在 CI/CD 流水线中实现自动化部署回滚？请描述你的实现方案。

> 面试官您好，我在之前的 Jenkins CI/CD 项目中实现了 **手动 + 自动结合** 的部署回滚方案，核心思路是**先做严格的版本化，再基于版本实现回滚**。

### 一、实现回滚的 3 个核心前提（没有这些，回滚根本做不了）

1. **严格的镜像版本化**：绝对禁止使用`latest`标签，每次构建都生成唯一的版本号（我用的是`Git短commit号+Jenkins构建号`，比如`a1b2c3d-45`），每个版本对应一个唯一的 Docker 镜像
2. **留存完整部署记录**：每次部署都记录版本号、镜像地址、部署时间、提交人、部署结果，保留最近 10 个历史版本
3. **标准化健康检查**：能准确判断应用是否正常运行（比如 HTTP 状态码、核心接口返回值、端口连通性）

---

### 二、手动回滚实现（最常用，基础必备）

这是所有回滚方案的基础，用户可以在 Jenkins 上一键选择要回滚的版本。

#### 具体实现步骤

1. 在 Jenkins 流水线中开启 **参数化构建** ，添加一个字符串参数`ROLLBACK_VERSION`，默认值为空
2. 流水线逻辑判断：
    
    - 如果`ROLLBACK_VERSION`为空：执行正常流程（拉取代码→构建→打包镜像→推送仓库→部署）
    - 如果`ROLLBACK_VERSION`不为空：跳过构建打包步骤，直接拉取对应版本的镜像进行部署
    
3. 回滚完成后，执行健康检查，确认回滚成功，发送通知

#### 核心 Pipeline 示例（简化版）

```groovy
pipeline {
    agent any
    parameters {
        string(name: 'ROLLBACK_VERSION', defaultValue: '', description: '要回滚的版本号，留空执行正常部署')
    }
    stages {
        stage('判断是否回滚') {
            steps {
                script {
                    if (params.ROLLBACK_VERSION) {
                        echo "开始回滚到版本：${params.ROLLBACK_VERSION}"
                        IMAGE_TAG = params.ROLLBACK_VERSION
                    } else {
                        echo "执行正常部署"
                        IMAGE_TAG = "${gitCommitShort}-${BUILD_NUMBER}"
                        // 执行拉取代码、构建、打包镜像、推送仓库的步骤
                    }
                }
            }
        }
        stage('部署应用') {
            steps {
                // 拉取指定版本的镜像，启动容器
                sh "docker pull my-registry.com/my-app:${IMAGE_TAG}"
                sh "docker stop my-app && docker rm my-app"
                sh "docker run -d --name my-app -p 8080:80 my-registry.com/my-app:${IMAGE_TAG}"
            }
        }
        stage('健康检查') {
            steps {
                sh "sleep 10"
                sh "curl -f http://localhost:8080/health || exit 1"
            }
        }
    }
    post {
        success {
            echo "部署/回滚成功，版本：${IMAGE_TAG}"
            // 发送企业微信通知
        }
        failure {
            echo "部署/回滚失败，版本：${IMAGE_TAG}"
        }
    }
}
```

---

### 三、自动回滚实现（进阶，体现技术深度）

当新版本部署失败或健康检查不通过时，系统自动回滚到上一个稳定版本，无需人工干预。

#### 具体实现步骤

1. 每次成功部署后，把当前版本号写入一个文件（比如`/data/last_stable_version`），作为上一个稳定版本
2. 新版本部署完成后，执行**3 次健康检查**（每次间隔 5 秒），只要有一次失败就判定为部署失败
3. 部署失败时，自动读取`last_stable_version`文件中的版本号，触发回滚流程
4. 回滚完成后，再次执行健康检查，确认回滚成功，发送紧急通知

#### 核心 Pipeline 示例（自动回滚部分）

```groovy
stage('健康检查与自动回滚') {
    steps {
        script {
            def healthCheckPassed = false
            // 重试3次健康检查
            for (int i=0; i<3; i++) {
                sh "sleep 5"
                if (sh(script: "curl -f http://localhost:8080/health", returnStatus: true) == 0) {
                    healthCheckPassed = true
                    break
                }
            }
            
            if (!healthCheckPassed) {
                echo "健康检查失败，开始自动回滚"
                // 读取上一个稳定版本
                def lastStableVersion = sh(script: "cat /data/last_stable_version", returnStdout: true).trim()
                // 执行回滚
                sh "docker pull my-registry.com/my-app:${lastStableVersion}"
                sh "docker stop my-app && docker rm my-app"
                sh "docker run -d --name my-app -p 8080:80 my-registry.com/my-app:${lastStableVersion}"
                // 再次检查回滚是否成功
                sh "sleep 10 && curl -f http://localhost:8080/health"
                error "自动回滚已完成，请排查新版本问题"
            } else {
                echo "健康检查通过，更新稳定版本"
                sh "echo ${IMAGE_TAG} > /data/last_stable_version"
            }
        }
    }
}
```

---

### 四、补充得分点（一句话带过，体现实操经验）

> 我当时还做了几个优化：
> 
> 1. 数据库回滚单独处理，因为数据库回滚风险高，不做自动回滚，只提供手动回滚脚本
> 2. 结合灰度发布，先部署到 10% 的节点，健康检查通过后再全量部署，降低故障影响范围
> 3. 自动回滚会发送紧急通知到运维群，同时记录回滚日志，方便后续排查问题

### 模块 5：监控与日志

### Prometheus 和 Zabbix 在架构和适用场景上有什么核心区别？你会如何选择使用这两个监控工具？

>  
- Prometheus 是 **拉模型（Pull）** 为主，主动去拉取监控数据
- Zabbix 是 **推模型（Push）** 为主，Agent 主动上报数据，也支持被动拉取

> 面试官您好，Prometheus 和 Zabbix 的核心区别和我的选择标准如下：

#### 一、核心区别（3 个最关键的）

|维度|Prometheus|Zabbix|
|---|---|---|
|数据模型|时序数据库，支持标签（Label），数据查询和聚合能力极强|关系型数据库（MySQL/PostgreSQL），数据查询能力弱|
|架构模型|拉模型为主，Prometheus Server 主动拉取 Exporter 的数据|推模型为主，Agent 主动上报数据，也支持被动拉取|
|适用场景|云原生、容器化、微服务架构，适合监控动态变化的资源|传统物理机、虚拟机架构，适合监控静态的基础设施|

#### 二、我的选择标准

1. **如果是云原生 / K8s 环境**：优先选 Prometheus，它和 K8s 无缝集成，能自动发现 Pod、Service 等动态资源，这是 Zabbix 做不到的
2. **如果是传统物理机 / 虚拟机环境**：优先选 Zabbix，它开箱即用，内置了很多主机、网络、数据库的监控模板，部署和维护成本低
3. **大型混合环境**：可以两者结合，用 Zabbix 监控基础设施，用 Prometheus 监控容器和微服务

### ELK 栈的三个核心组件分别负责什么功能？如果发现日志没有收集到 Elasticsearch 中，你会如何排查？

> 面试官您好，ELK 栈的三个核心组件功能和日志收集失败的排查流程如下：

#### 一、ELK 三个核心组件的功能

1. **Elasticsearch（ES）**：分布式搜索引擎，负责 **存储、索引和查询** 所有日志数据，是 ELK 的核心
2. **Logstash**：数据处理管道，负责 **收集、过滤、转换** 日志数据，把不同格式的日志统一成标准格式后发送给 ES
3. **Kibana**：可视化平台，负责把 ES 中的数据以图表、仪表盘的形式展示出来，支持日志检索和分析

> 补充：现在生产环境一般会加 **Filebeat** 作为轻量日志收集器，部署在应用服务器上，代替 Logstash 收集日志，因为 Filebeat 占用资源极少，性能更好。

#### 二、日志没有收集到 ES 的排查流程（从源到目标，按顺序排查）

1. **第一步：检查应用是否正常生成日志**
    
    - 登录应用服务器，查看日志文件是否存在，是否有新内容写入
    - 检查日志文件的权限，确保 Filebeat 有读取权限
    
2. **第二步：检查 Filebeat 是否正常运行**
    
    - 用`systemctl status filebeat`查看 Filebeat 进程状态
    - 查看 Filebeat 日志：`tail -f /var/log/filebeat/filebeat.log`，看是否有连接失败、权限错误等信息
    - 检查 Filebeat 配置文件，确认日志路径和输出地址是否正确
    
3. **第三步：检查 Logstash 是否正常运行**
    
    - 用`systemctl status logstash`查看 Logstash 进程状态
    - 查看 Logstash 日志：`tail -f /var/log/logstash/logstash-plain.log`，看是否有数据接收错误、格式转换错误
    - 检查 Logstash 配置文件，确认输入端口和输出 ES 地址是否正确
    
4. **第四步：检查 Elasticsearch 是否正常运行**
    
    - 用`curl http://localhost:9200`测试 ES 是否能正常访问
    - 查看 ES 日志：`tail -f /var/log/elasticsearch/elasticsearch.log`，看是否有磁盘满、索引创建失败等问题
    - 用`curl http://localhost:9200/_cat/indices`查看是否有对应的日志索引生成

### 如何使用 Prometheus 监控 MySQL 和 Redis 的运行状态？需要用到哪些组件？

> 面试官您好，Prometheus 监控 MySQL 和 Redis 的核心是使用**Exporter（导出器）**，具体步骤和需要的组件如下：

#### 一、核心原理

Prometheus 本身不直接监控数据库，而是通过 Exporter 来获取数据库的监控指标。Exporter 是一个独立的程序，它会连接数据库，把数据库的运行状态转换成 Prometheus 能识别的时序数据格式，然后 Prometheus 主动拉取这些数据。

#### 二、需要用到的组件

- 监控 MySQL：**mysqld_exporter**
- 监控 Redis：**redis_exporter**
- 核心组件：Prometheus Server + Grafana（可视化）

#### 三、具体实现步骤（以 MySQL 为例，Redis 完全一样）

1. **部署 mysqld_exporter**
    
    - 在 MySQL 服务器上下载并解压 mysqld_exporter
    - 创建一个 MySQL 监控用户，授予必要的权限
    - 启动 mysqld_exporter，指定 MySQL 的连接地址和账号密码
    - 默认监听 9104 端口，访问`http://MySQL服务器IP:9104/metrics`可以看到监控指标
    
2. **配置 Prometheus 拉取数据**
    
    - 修改 Prometheus 的配置文件`prometheus.yml`，添加一个新的 job：
        
        ```yaml
        scrape_configs:
          - job_name: 'mysql'
            static_configs:
              - targets: ['MySQL服务器IP:9104']
        ```
        
    - 重启 Prometheus，让配置生效
    
3. **Grafana 可视化**
    
    - 在 Grafana 中添加 Prometheus 数据源
    - 导入 MySQL 监控的官方仪表盘（ID：7362），就能看到 MySQL 的连接数、QPS、慢查询数、缓存命中率等所有核心指标
    
4. **配置告警**
    
    - 在 Prometheus 中配置告警规则，比如 MySQL 连接数超过 80%、主从同步延迟超过 10 秒时发送告警

## 三、项目经验深挖题（重点考察，考察实际项目能力）

### 针对《基于 Jenkins 的 CI/CD 系统》项目

1. 这个项目中你独立负责了哪些核心模块？遇到的最大技术难点是什么？你是如何分析并解决的？
2. 你提到通过集成 WebHook 实现部署结果实时推送，具体是如何实现的？推送的渠道有哪些？如果推送失败，如何处理？
3. 你使用 Docker 多阶段构建解决了什么问题？相比单阶段构建，镜像大小和构建时间分别有多少优化？
4. 这个 CI/CD 系统支持哪些语言的项目部署？如何实现不同语言项目的标准化构建和部署？
5. 你是如何处理部署过程中出现的部分节点失败的情况？有没有实现灰度发布？如果有，是如何实现的？
6. 项目上线后，除了故障响应效率提升 80%，还有哪些可量化的收益？如果现在要对这个系统进行升级，你会优先优化哪些方面？

## 四、工作经历深挖题（考察落地能力与软技能）

### 针对广东海信宽带科技有限公司工作经历

1. 你开发的产线首件自动化上传系统，具体的技术架构是什么？使用了哪些 Python 库？在图片处理和系统对接环节遇到了什么问题？
2. 你将单流程耗时从 2 小时压缩至 40 分钟，主要优化了哪些环节？人工操作失误率从 12% 降至 0 是如何实现的？
3. 你搭建的标准化运维 SOP 体系具体包含哪些内容？是如何组织和更新的？如何确保一线运维人员严格执行 SOP？
4. 新人培训周期从 6 个月缩短至 2 个月，你采取了哪些具体措施？有没有配套的考核机制？
5. 在设备运维工作中，你遇到过最严重的一次生产故障是什么？请按照 "问题现象 - 排查思路 - 解决过程 - 总结反思" 的逻辑详细说明。
6. 你认为工业设备运维和互联网运维最大的区别是什么？你如何将设备运维的经验迁移到互联网运维工作中？

## 五、高阶架构与综合能力题（区分度题，考察技术深度与思维）

1. 请设计一个高可用的 MySQL 一主两从集群架构，需要考虑哪些核心因素？如何实现自动故障转移？
2. 设计一个企业级的监控告警体系，需要覆盖哪些维度？如何避免告警风暴？
3. 如果线上突然出现大面积服务不可用，你的完整应急处理流程是什么？
4. 你对 DevOps 的理解是什么？在实际工作中，你是如何践行 DevOps 理念的？
5. 你为什么想要从设备运维转向互联网运维？你认为自己在互联网运维岗位上的核心优势是什么？还有哪些需要提升的地方？

## 六、编程实操题（二选一，限时 15 分钟）

1. **Shell 题**：编写一个脚本，监控服务器的 CPU 使用率（1 分钟平均）、内存使用率、根分区磁盘使用率，当任意一项超过 80% 时，发送邮件告警（邮件内容包含服务器 IP、超标指标、当前数值和告警时间）。
2. **Python 题**：使用 FastAPI 编写一个接口`/api/server/status`，实现查询服务器的 CPU 使用率、内存使用率和磁盘使用率，并返回 JSON 格式的结果。