## 一、基础入门篇

1. 请写出在`/home`目录下创建一个名为`test_dir`的文件夹，再在该文件夹内创建一个名为`test_file.txt`的空文件的完整命令。

```bash
sudo mkdir /home/test_dir && sudo touch /home/test_dir/test_file.txt
```

2. 如何查看当前所在的工作目录？如何切换到`/root`目录？

```shell
# 查看当前所在的工作目录
pwd
# 切换到/root目录
cd /root
```

3. 请写出将`test_file.txt`的权限设置为：所有者可读可写可执行，所属组可读可执行，其他用户仅可读的**数字权限命令**和**符号权限命令**。

```bash
# 数字权限命令
sudo chmod 754 test_file.txt
# 符号权限命令
sudo chmod u=rwx,g=rx,o=r test_file.txt
```

4. 如何查看`test_file.txt`文件的详细属性（包括权限、所有者、大小、修改时间等）？

```bash
ls -l test_file.txt
# 别名简写（多数系统默认支持）
ll test_file.txt
```

5. 请写出将`/home/test_dir`目录及其内所有文件，复制到`/tmp`目录下的命令。

```bash
sudo cp -r /home/test_dir /tmp
```

6. 如何查看当前系统中所有正在运行的进程？

```bash
# 方式1（BSD风格，详细信息）
ps aux
# 方式2（标准System V风格）
ps -ef
```

7. 请写出查看`/var/log/messages`文件的最后 20 行内容的命令。

```bash
tail -n 20 /var/log/messages
```

8. 如何在当前目录下查找所有后缀为`.log`的文件？

```bash
# 推荐方式（精准查找，支持递归）
find . -name "*.log"
# 管道过滤方式（仅当前目录）
ls -la | grep "\.log$"
```

9. 请写出删除`/tmp/test_dir`目录及其内所有内容的命令。

```shell
sudo rm -rf /tmp/test_dir
```

10. 如何给`test_file.txt`文件创建一个软链接（符号链接）到`/root`目录下，链接名为`file_link`？

```bash
sudo ln -s /home/test_dir/test_file.txt /root/file_link
```

---

## 二、进阶操作篇

1. 请写出一条命令，统计当前目录下所有`.txt`文件的总行数。

```bash
cat *.txt | wc -l
```

2. 如何将`ls -l`命令的执行结果，**覆盖写入**到当前目录的`list.txt`文件中？如何**追加写入**？

```bash
# 覆盖写入
ls -l > list.txt
# 追加写入
ls -l >> list.txt
```

3. 请写出使用 grep 命令，在`/var/log`目录下所有`.log`文件中，查找包含`error`关键词的行，且忽略大小写。

```bash
# 基础版（仅/var/log目录下的.log文件）
grep -i "error" /var/log/*.log
# 进阶版（递归遍历/var/log下所有子目录的.log文件）
grep -ir "error" /var/log --include="*.log"
```

4. 如何将当前目录下的`test_dir`文件夹打包并压缩为`test_dir.tar.gz`文件？如何解压该文件到`/opt`目录？

```bash
# 打包并压缩为test_dir.tar.gz
tar -zcvf test_dir.tar.gz test_dir
# 解压到/opt目录
tar -zxvf test_dir.tar.gz -C /opt
```

5. 请写出一条命令，找出当前系统中 CPU 占用率最高的前 5 个进程。

```bash
ps aux --sort=-%cpu | head -5
```

6. 如何查看系统的磁盘分区使用情况？如何查看某个目录的总占用大小？

```bash
# 查看系统磁盘分区使用情况
df -h
# 查看指定目录的总占用大小（替换为目标目录路径）
du -sh 目标目录路径
```

7. 请使用 awk 命令，打印出`/etc/passwd`文件中所有用户的用户名和对应的 UID。

```bash
awk -F: '{print $1, $3}' /etc/passwd
```

8. 如何临时关闭系统的防火墙？如何永久关闭防火墙（分别写出 RHEL/CentOS 系列和 Debian/Ubuntu 系列的命令）？

```bash
# ========== RHEL/CentOS 7+ 系列（firewalld防火墙） ==========
# 临时关闭防火墙
sudo systemctl stop firewalld
# 永久关闭防火墙
sudo systemctl disable --now firewalld

# ========== Debian/Ubuntu 系列（ufw防火墙） ==========
# 临时/永久关闭防火墙（命令本身永久生效）
sudo ufw disable
```

9. 请写出查看系统内存使用情况的命令（至少 2 种）。

```bash
# 方式1：人性化显示内存概览
free -h
# 方式2：查看内核级详细内存信息
cat /proc/meminfo
# 方式3：实时统计内存与系统负载
vmstat
```

10. 如何给一个正在运行的进程发送终止信号？如果进程无法正常终止，该使用什么信号强制终止？

```bash
# 正常终止进程（发送15号SIGTERM信号，允许进程优雅退出，替换为目标进程PID）
kill 进程PID
# 强制终止进程（发送9号SIGKILL信号，无法被进程捕获，替换为目标进程PID）
kill -9 进程PID
```

---

## 三、Shell 脚本与自动化篇

1. 请编写一个简单的 Shell 脚本，判断`/root/test_file.txt`文件是否存在，若存在则输出`文件存在`，若不存在则输出`文件不存在`。

```bash
#!/bin/bash
TARGET_FILE="/root/test_file.txt"
if [ -f "${TARGET_FILE}" ]; then
    echo "文件存在"
else
    echo "文件不存在"
fi
```

2. 如何设置一个定时任务，每天凌晨 2 点执行`/root/backup.sh`脚本？

```bash
# 1. 编辑当前用户的定时任务配置
crontab -e
# 2. 在配置文件中添加以下行，保存退出后生效
0 2 * * * /root/backup.sh
```

3. 请编写一个 Shell 脚本，遍历当前目录下所有`.log`文件，将每个文件的文件名打印出来，并统计每个文件的行数。

```bash
#!/bin/bash
for log_file in *.log; do
    if [ -f "${log_file}" ]; then
        echo "文件名：${log_file}"
        line_num=$(wc -l < "${log_file}")
        echo "文件行数：${line_num}"
        echo "-------------------------"
    fi
done
```

4. 如何查看当前用户的所有定时任务？如何删除当前用户的所有定时任务？

```bash
# 查看当前用户的所有定时任务
crontab -l
# 删除当前用户的所有定时任务（谨慎使用，无二次确认）
crontab -r
```

5. 请编写一个 Shell 脚本，接收一个数字参数 n，计算 1 到 n 的累加和并输出。

```bash
#!/bin/bash
if [ $# -ne 1 ]; then
    echo "使用方法：sh $0 数字参数，例：sh $0 100"
    exit 1
fi
max_num=$1
sum=0
for ((i=1; i<=max_num; i++)); do
    sum=$((sum + i))
done
echo "1到${max_num}的累加和为：${sum}"
```

6. 如何在 Shell 脚本中定义一个变量，并将命令的执行结果赋值给该变量？请举例说明。

```bash
#!/bin/bash
# 方式1：$() 格式（推荐，可读性强，支持嵌套）
current_time=$(date "+%Y-%m-%d %H:%M:%S")
echo "当前系统时间：${current_time}"

# 方式2：反引号格式（兼容旧版Shell）
current_user=`whoami`
echo "当前登录用户：${current_user}"
```

7. 请编写一个 Shell 脚本，判断当前登录用户是否为 root，若不是则输出`请使用root用户执行`并退出脚本。

```bash
#!/bin/bash
if [ $UID -ne 0 ]; then
    echo "请使用root用户执行"
    exit 1
fi
echo "当前为root用户，脚本正常执行"
```

8. 如何设置定时任务，每周一到周五的 18 点 30 分，执行一次`/root/clean_log.sh`脚本？

```bash
# 1. 编辑当前用户的定时任务配置
crontab -e
# 2. 在配置文件中添加以下行，保存退出后生效
30 18 * * 1-5 /root/clean_log.sh
```

9. 请编写一个 Shell 脚本，批量创建 10 个用户，用户名分别为 user01 到 user10，并设置初始密码为 123456。

```bash
#!/bin/bash
for user_suffix in {01..10}; do
    user_name="user${user_suffix}"
    # 创建用户
    useradd "${user_name}"
    # RHEL/CentOS系列使用以下行设置密码
    echo "123456" | passwd --stdin "${user_name}"
    # Debian/Ubuntu系列注释上一行，启用以下行
    # echo "${user_name}:123456" | chpasswd
    echo "用户${user_name}创建完成，初始密码为123456"
done
```

10. 如何让一个 Shell 脚本在后台执行，即使关闭终端也不会停止运行？请写出至少 2 种实现方式。

```bash
# ========== 方式1：nohup + & 组合（无需额外安装，推荐） ==========
# 脚本后台执行，日志输出到script_run.log，关闭终端不停止
nohup /root/your_script.sh > /root/script_run.log 2>&1 &

# ========== 方式2：screen 终端复用工具（支持随时重连查看进度） ==========
# 1. 安装screen
# RHEL/CentOS：sudo yum install screen -y
# Debian/Ubuntu：sudo apt install screen -y
# 2. 创建专属会话
screen -S script_run
# 3. 执行脚本
/root/your_script.sh
# 4. 按 Ctrl+A+D 脱离会话，关闭终端不影响执行
# 5. 重新连接会话查看进度：screen -r script_run
```

---

## 四、系统管理与运维篇

1. 如何查看本机的 IP 地址？请写出至少 2 种不同的命令。

```bash
# 方式1：通用推荐命令（iproute2工具集，系统默认自带）
ip addr
# 方式2：传统兼容命令（需安装net-tools工具包）
ifconfig
# 方式3：极简输出仅IP地址
hostname -I
```

2. 如何启动、停止、重启、查看 nginx 服务的运行状态？请写出 systemd 系统的完整命令。

```bash
# 启动nginx服务
sudo systemctl start nginx
# 停止nginx服务
sudo systemctl stop nginx
# 重启nginx服务
sudo systemctl restart nginx
# 查看nginx服务运行状态
sudo systemctl status nginx
```

3. 请写出查看系统监听的所有 TCP 端口的命令。

```bash
# 通用推荐命令
ss -tlnp
# 传统兼容命令（需安装net-tools工具包）
netstat -tlnp
# 参数说明：-t TCP协议 -l 监听状态 -n 数字格式显示端口/IP -p 显示关联进程与PID
```

4. 如何给系统新增一个名为`dev`的用户组，并将 user01 用户添加到该用户组中？

```bash
# 新增dev用户组
sudo groupadd dev
# 将user01用户添加到dev用户组
sudo usermod -aG dev user01
```

5. 如何查看系统的内核版本？如何查看系统的发行版版本？

```bash
# ========== 查看内核版本 ==========
# 简洁版
uname -r
# 详细版
uname -a

# ========== 查看发行版版本 ==========
# RHEL/CentOS系列
cat /etc/redhat-release
# Debian/Ubuntu系列
cat /etc/os-release
# 通用兼容命令
lsb_release -a
```

6. 请写出临时配置网卡 IP 地址为 192.168.1.100/24，网关为 192.168.1.1 的命令。

```bash
# 示例网卡名为eth0，请根据实际网卡名修改
# 配置IP地址与子网掩码
sudo ip addr add 192.168.1.100/24 dev eth0
# 配置默认网关
sudo ip route add default via 192.168.1.1 dev eth0
```

7. 如何查看系统的开机启动项？如何设置 nginx 服务开机自启？如何关闭该服务的开机自启？

```bash
# 查看系统所有已启用的开机自启服务
sudo systemctl list-unit-files --type=service | grep enabled
# 设置nginx服务开机自启
sudo systemctl enable nginx
# 关闭nginx服务的开机自启
sudo systemctl disable nginx
```

8. 请写出挂载`/dev/sdb1`分区到`/mnt/data`目录的命令，以及设置开机自动挂载的完整方法。

```bash
# ========== 临时挂载 ==========
# 先创建挂载目录
sudo mkdir -p /mnt/data
# 执行挂载
sudo mount /dev/sdb1 /mnt/data

# ========== 开机自动挂载配置 ==========
# 1. 查看分区UUID（避免设备名变动导致挂载失败）
blkid /dev/sdb1
# 2. 编辑fstab配置文件
sudo vim /etc/fstab
# 3. 在文件末尾添加以下配置（替换UUID为实际查询到的值，文件系统格式根据实际修改）
UUID=xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx /mnt/data ext4 defaults 0 0
# 4. 验证配置是否正确，无报错则配置生效，重启后自动挂载
sudo mount -a
```

9. 如何查看系统的登录日志，找出最近登录失败的用户记录？

```bash
# 直接查看所有登录失败记录
lastb
# RHEL/CentOS系列查看登录失败详情
grep "Failed password" /var/log/secure
# Debian/Ubuntu系列查看登录失败详情
grep "Failed password" /var/log/auth.log
```

10. 当系统出现磁盘空间满的告警，请写出你的排查步骤、核心命令和常规处理思路。

```bash
# ========== 排查步骤与核心命令 ==========
# 1. 定位满额磁盘分区，确认使用率100%的分区
df -h
# 2. 定位根目录下高占用的目录，按大小降序排序
du -sh /* | sort -rh
# 3. 逐层深入定位大文件（示例：排查/var目录）
du -sh /var/* | sort -rh
# 4. 快速查找100M以上的超大文件
find / -type f -size +100M
# 5. 排查已删除但未被进程释放的文件
lsof | grep deleted

# ========== 常规处理思路 ==========
# 1. 安全清理无业务影响的文件
# 清理临时文件
rm -rf /tmp/*
# 清理包管理器缓存（RHEL/CentOS）
yum clean all
# 清理包管理器缓存（Debian/Ubuntu）
apt clean
# 清理过期日志、无用备份/缓存文件

# 2. 释放已删除未释放的空间
# 重启占用文件的对应进程，即可释放磁盘空间

# 3. 长期优化
# 配置logrotate日志轮转，避免日志无限增长
# 设置备份文件过期自动清理策略

# 4. 扩容处理
# 若业务文件均为必要文件，通过LVM逻辑卷扩容、新增磁盘挂载业务目录解决空间不足问题
```